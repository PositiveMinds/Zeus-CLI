# Rust
/target/
**/*.rs.bk

# Agent local overrides (personal / machine-specific)
**/.agent/settings.local.toml
settings.local.toml

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
*.pem
*.key

# Logs and local caches (if ever checked in by mistake)
*.log

# Local smoke-test home
/tmp-agent-home/

# Machine-local agent state (checkpoints, sessions)
**/.agent/
.claude/
**/zeus-home/

# Standalone UI/design prototypes (not part of the zeus project)
/supercode ui/
*.webp

# Scratch/research notes and media (Word docs, videos, one-off scripts)
*.docx
*.mp4
tmp-ranked.ps1

# npm platform packages — the release workflow drops the actual prebuilt
# binary into each of these right before `npm publish`; never checked in.
npm/zeus-code-*/bin/zeus
npm/zeus-code-*/bin/zeus.exe

# Internal design docs — kept out ahead of the repo going public. Note:
# removing these from the tree doesn't scrub them from existing git history;
# a full history rewrite would be needed for that.
AI_Coding_Agent_Blueprint_Database_Free.md
AI_Coding_Agent_Design_Tension_Analysis.md
AI_Coding_Agent_Unique_Premium_Features.md
AI_Multi_Agent_Company_Architecture.md
